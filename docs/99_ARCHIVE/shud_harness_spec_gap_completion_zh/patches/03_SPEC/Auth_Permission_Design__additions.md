# Patch: Auth_Permission_Design additions

**目标文件：** `docs/03_SPEC/Auth_Permission_Design.md`

## 插入位置：角色和 Session 章节后

新增：

```markdown
User、Session、PermissionDecision、AuditEvent 和 AgentIdentity 的 schema 见 `User_Session_And_Audit_Schema.md`。
```

## 插入位置：API key 管理后

新增：

```markdown
SecretRef、ConfigRecord、环境变量命名和 redaction policy 见 `Config_Secrets_And_Environment_Spec.md`。
```
