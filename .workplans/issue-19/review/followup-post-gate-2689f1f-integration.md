Reviewer agent: review-integration
Review round: post-gate follow-up after V73 fixes
Reviewed head SHA: 2689f1f9bb82b23a86acd51418e40f8fafba3d04

Summary: Not clean; V73 fixes cover the named regressions, but scan-budget truncation and lifecycle containment still leave candidate blocking evidence/audit gaps.

Invariant Matrix Coverage:
- Governing invariant: missing - OS raw bytes remain protected, but a hidden interpreter raw-write attempt can still be reported as allowed.
- Source-of-truth identity/contract: covered for recognized denials - result/audit/WS builder share rule, decision, remediation, profile id, and invocation id.
- Producers: missing - interpreter call scanning silently truncates and lifecycle containment depends on static session-escape strings plus parent-graph sampling.
- Validators/preflight: missing - no regression for raw write after the 512th helper call or obfuscated detached child process.
- Storage/cache/query: missing - canonical audit evidence can still be moved after a successful wrapper completion by an untracked detached descendant.
- Public routes/entrypoints: covered - guarded entrypoint remains the SHUD-owned `bash` tool `run()` path.
- Frontend/downstream consumers: missing - false allowed completion propagates to running metadata and downstream UI as success.
- Evidence/audit/readiness: missing - both findings can produce success/allowed evidence for unsafe execution state.
- Zero cleanliness: covered - `zero` diff is clean and pinned at `13e25c116c62411e6ee8a0ad67a6c53dc7c376c6`.

Findings:
- Severity: P1 / Failure class: hidden-denial evidence and scan-budget truncation / Contract or invariant: interpreter payload raw-write attempts must produce remediation-shaped failure plus audit evidence; bounded scans must not silently fail open for hidden-denial-sensitive forms / Evidence: `packages/core/src/tools/raw-data-sandbox.ts:50`, `packages/core/src/tools/raw-data-sandbox.ts:605`, `packages/core/src/tools/raw-data-sandbox.ts:677`, `packages/core/src/tools/raw-data-sandbox.ts:2513`, `packages/core/src/tools/raw-data-sandbox.ts:3397` / Scenario or repro: a Python payload under the 32K payload limit performs 512 benign `open("workspace/noopN.txt","w")` calls, then `try: open("data/raw/late.txt", mode="w").write("x") except Exception: pass`, with stderr suppressed. `findCallArgumentLists()` stops at 512 calls without returning a budget-exceeded state, so the protected raw target after the cap is missed and the swallowed OS denial can become `tool.completed` / Consequence: raw bytes are not mutated, but the tool result, audit row, running metadata, and WS input can incorrectly represent a denied raw mutation attempt as allowed / Fix direction: make call-count exhaustion part of `RawDataCommandAnalysis.budgetExceeded` or scan within a bounded complete budget; for hidden-sensitive over-budget forms, fail closed with `denied_by_sandbox` evidence before execution / Required verification: add unit and sandbox regressions with >512 interpreter helper calls followed by a hidden raw write; assert `denied_by_sandbox`, no raw mutation, `tool.failed` audit, and final running metadata failure / Sibling surfaces: Node/Ruby/Rscript helper scanners, advisory evaluation, post-exec denial normalization / Blocks merge: Yes.
- Severity: P1 / Failure class: process lifecycle containment and audit durability / Contract or invariant: wrapper completion must not leave invocation-owned descendants able to mutate workspace/audit evidence after success; audit output must bind to the exact profile/run / Evidence: `packages/core/src/tools/raw-data-sandbox.ts:424`, `packages/core/src/tools/raw-data-sandbox.ts:1528`, `packages/core/src/tools/raw-data-sandbox.ts:1583`, `packages/core/src/tools/raw-data-sandbox.ts:1647`, `packages/core/src/tools/raw-data-sandbox.ts:3283` / Scenario or repro: a bash command runs Python that spawns a delayed child with an obfuscated detached-session keyword such as `{"start_new" + "_session": True}`, then exits. The literal regex in `hasSessionEscapeSignal()` does not match, there is no shell `&`, and the descendant tracker only discovers processes still connected through the sampled parent graph. If the child is reparented before sampling, the wrapper can append `tool.completed` and return while the child later moves `workspace/tasks` / Consequence: canonical policy-gate audit evidence can be moved/lost after the wrapper has reported success, so result, audit, WS/event input, and running metadata no longer describe a durable completed run / Fix direction: make containment independent of static session-escape string matching and parent-graph timing, or protect the audit namespace outside sandbox-writable state; fail closed when detached descendants cannot be proven contained / Required verification: add allowed, timeout, and abort regressions with an obfuscated detached child that delays `mv workspace/tasks workspace/tasks.moved`; assert either stable fail-closed evidence or canonical audit path durability after settle / Sibling surfaces: audit ancestor moves, delayed background work, allowed workspace writes, timeout/abort cleanup, running metadata / Blocks merge: Yes.

Non-blocking notes:
- `git diff --check` passed; `openspec validate m1-foundation --strict --no-interactive` passed; `git -C zero diff --quiet` passed.
- I did not run the Bun suites because `bun` is not on PATH in this shell.
- Wrapper faithfulness note: `command: ""` now returns “Sandboxed bash missing command,” while Zero `BashTool` would execute an empty shell command successfully; document or test this if empty-command compatibility matters.
