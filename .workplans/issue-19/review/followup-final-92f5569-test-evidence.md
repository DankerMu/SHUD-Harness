Reviewer agent: review-test-evidence
Review round: final comprehensive follow-up 92f5569
Reviewed head SHA: 92f556915416a57015dcaa32ca97e044c9fc3353
Summary: One P2 evidence gap remains around malformed direct custom policy evaluators; the raw byte-authority, audit/WS, macOS seatbelt, and prior b999d2e fixes are otherwise covered.

Invariant Matrix Coverage:
- Central SHUD tool wrapper / Zero unchanged: covered - registry wrapping and unwrapped-tool failure are tested at `packages/core/src/tools/policy-gate-registry.test.ts:266` and `:293`; SHUD runtime replaces Zero bash with `RawDataSandboxedBashTool` at `packages/core/src/tools/policy-gate-registry.ts:157`; CI verifies `zero` is pinned/clean in `.github/workflows/ci.yml:42`.
- `data/raw/**` byte authority via macOS seatbelt: covered - profile denies `file-write*` on protected raw/evidence paths at `packages/core/src/tools/raw-data-sandbox.ts:270`; execution uses absolute `/usr/bin/sandbox-exec` with the generated profile at `packages/core/src/tools/raw-data-sandbox.ts:1711`; macOS `macos-seatbelt` check is passing for run `28749277620`.
- Six raw mutation escape classes: covered - interpreter payload, pipeline/stdin, dynamic target, child/grandchild state, symlink/`../`, and rename/unlink cases are defined at `packages/core/src/tools/raw-data-sandbox.test.ts:935` and executed at `:985`; additional byte-preservation coverage exists for in-place edits and redirections at `:1836`, `:1867`, `:1897`, `:1940`, and `:1968`.
- Legal raw reads and workspace writes: covered - raw read succeeds at `packages/core/src/tools/raw-data-sandbox.test.ts:1986`, raw-read-to-workspace compatibility at `:2037`, `:2265`, `:2291`, `:2312`, and allowed workspace write at `:2484`; registry integration covers raw read/workspace write through the SHUD runtime bash at `packages/core/src/tools/policy-gate-registry.test.ts:468`.
- Stable root resolution and cwd drift: covered - relative runtime roots require `pathResolutionRoot` and fail closed without it at `packages/core/src/tools/raw-data-sandbox.test.ts:636`, `:672`, `:707`, `:742`, `:810`; registry propagation is covered at `packages/core/src/tools/policy-gate-registry.test.ts:573` and `:608`.
- Advisory layer fail-open plus navigable deny: covered - advisory only denies known static raw writes at `packages/core/src/tools/raw-data-sandbox.ts:970`; budget exhaustion returns non-denial analysis at `:997`; tests cover copy source/destination distinction and over-budget legal/hidden cases at `packages/core/src/tools/raw-data-sandbox.test.ts:3272`, `:3300`, `:3345`, `:4731`, `:4755`, `:4790`.
- Trusted raw-denial telemetry, remediation, audit, WS skeleton: covered - trusted evidence is created and bound to ToolResult at `packages/core/src/tools/raw-data-sandbox.ts:1178`; audit row conversion includes rule/decision/remediation/profile fields at `:1231`; WS trusted builder consumes only sandbox-owned ToolResult evidence at `packages/backend/src/ws/index.ts:55`; tests cover the positive event and rejection of caller-authored raw-denial shapes at `packages/backend/src/ws/index.test.ts:26`, `:109`, `:121`, `:165`, `:191`, `:300`.
- Post-exec stdout/stderr/exit status not overclaimed as `denied_by_sandbox`: covered - runtime post-exec only records lifecycle `allowed|failed` at `packages/core/src/tools/raw-data-sandbox.ts:702`; tests cover forged denial text, suppressed denials, unrelated permission failures, and over-budget visible raw writes at `packages/core/src/tools/raw-data-sandbox.test.ts:2007`, `:2064`, `:2127`, `:2183`, `:2207`, `:2236`, `:4814`, `:4842`.
- Outer `RAW_DATA_WRITE_RULE_ID` ownership for valid evaluator decisions: covered - outer raw denials become explicit misconfiguration results without profile/audit fabrication at `packages/core/src/tools/policy-gate-registry.ts:271` and `:357`; tests cover custom outer raw evaluator, advisory disabled, and root mismatch at `packages/core/src/tools/policy-gate-registry.test.ts:744`, `:773`, `:805`.
- Policy evaluator/remediation fail-closed for malformed direct evaluator returns: missing - thrown evaluators and invalid remediation through `createPolicyGateEvaluator` are covered at `packages/core/src/tools/policy-gate-registry.test.ts:98` and `:135`, but direct custom evaluator malformed deny objects are not validated after return; see finding below.
- Running handle lifecycle consistency: covered - raw sandbox finalizes handles at `packages/core/src/tools/raw-data-sandbox.ts:528` and `:1617`; policy wrapper finalizes denied/evaluator-error paths at `packages/core/src/tools/policy-gate-registry.ts:294`; tests cover allowed, afterExecute failure, visible failure, audit unavailable, timeout, and evaluator throw at `packages/core/src/tools/raw-data-sandbox.test.ts:2538`, `:2566`, `:2611`, `:2643`, `:2673` and `packages/core/src/tools/policy-gate-registry.test.ts:98`.
- Process inheritance / containment / waited foreground subprocess: covered - preflight rejects session escape and un-awaited background forms at `packages/core/src/tools/raw-data-sandbox.ts:4256`; tests cover real escape forms, waited Python Popen, un-awaited Popen, and over-budget preflight fail-open at `packages/core/src/tools/raw-data-sandbox.test.ts:2833`, `:2876`, `:3029`, `:3051`, `:3127`; arbitrary post-terminal descendant ownership is out-of-scope per `openspec/changes/m1-foundation/specs/policy-gate-spike/spec.md:52`.
- Pre-existing hardlink residual and bounded scan: covered - scan is limited to explicit protected roots and budgeted at `packages/core/src/tools/raw-data-sandbox.ts:1407`; residual demo and scan detection are tested at `packages/core/src/tools/raw-data-sandbox.test.ts:4443`; budget/path segment checks at `:4665`.
- Audit path safety and evidence lineage: covered - audit reservation rejects raw/symlink/hardlink/escape targets at `packages/core/src/tools/raw-data-sandbox.ts:4915`; tests cover raw audit roots, symlink/hardlink audit files, audit ancestor sabotage, canonical workspace layout, and raw byte preservation at `packages/core/src/tools/raw-data-sandbox.test.ts:4193`, `:4248`, `:4270`, `:4330`, `:4566`, `:4611`, `:4635`.
- Public export and downstream compatibility: covered - package-root test seams are not exported at `packages/core/src/tools/raw-data-sandbox.test.ts:119`; WS generic lifecycle failure remains legal at `packages/backend/src/ws/index.test.ts:228`; scoped spawn registry inherits sandboxed bash at `packages/core/src/tools/policy-gate-registry.test.ts:650`.
- Local and CI verification evidence: covered - `package.json:10` through `:14` covers typecheck, policy-gate, backend WS, and schema tests; `.github/workflows/ci.yml:17`, `:48`, and `:79` require Linux base, macOS seatbelt, and aggregate checks; GitHub reports `linux-base`, `macos-seatbelt`, and `check` success for head `92f556915416a57015dcaa32ca97e044c9fc3353`.

Findings:
- Severity: P2 / Failure class: test-evidence coverage gap and malformed extension-boundary error handling / Contract or invariant: policy evaluator and remediation failures should fail closed as a failed `ToolResult`, skip inner execution, run deny-style post-processing, and finish running handles; policy-deny evidence claims must not be overbroad / Scenario or repro: a direct custom `PolicyGateEvaluator` passed to `wrapToolWithPolicyGate` or `createShudRuntimeToolRegistry` returns `{ decision: "deny", ruleId: RAW_DATA_WRITE_RULE_ID, reason: "bad" } as any` or a deny with malformed `remediation`; because the evaluator returns normally, the catch at `packages/core/src/tools/policy-gate-registry.ts:257` is bypassed, `buildRawDataRuleMisconfiguredResult()` dereferences `decision.remediation.ref` at `packages/core/src/tools/policy-gate-registry.ts:361`, and the tool can reject instead of returning a failed `ToolResult` or finishing the running handle. Generic custom deny can also omit remediation because `buildPolicyGateDeniedResult()` only includes it if truthy at `packages/core/src/tools/policy-gate-registry.ts:383`. / Evidence: current tests cover evaluator throw at `packages/core/src/tools/policy-gate-registry.test.ts:98` and invalid remediation only when `createPolicyGateEvaluator()` throws at `:135`; direct custom evaluator malformed return is not covered, while custom evaluators are an exposed runtime option at `packages/core/src/tools/policy-gate-registry.ts:57`. / Consequence: the b999d2e fix evidence overstates the boundary; a malformed custom evaluator can still escape the failed-ToolResult lifecycle and leave observable running-tool state stale, or return a non-navigable generic denial without remediation. / Fix direction: validate the returned `PolicyGateDecision` at the wrapper boundary or wrap deny result construction in the same fail-closed/finalize path; require remediation for all deny decisions before building payloads. / Required verification: add focused tests where a direct custom evaluator returns malformed raw-rule deny and malformed generic deny; assert `success=false`, inner tool calls remain `0`, output is a failed policy-gate error with remediation or explicit configuration error, `afterExecute`/secret filtering runs, and the running handle is `finished`. / Sibling surfaces: `createShudRuntimeToolRegistry({ evaluate })`, `wrapToolWithPolicyGate()`, outer raw-rule misconfiguration, generic policy denials, future role/profile rule evaluators. / Blocks merge: no, but it should be fixed or explicitly deferred because it weakens the new fail-closed evidence claim.

Non-blocking notes:
- `Rscript` real seatbelt tests are conditional on local `Rscript` availability; this is acceptable for the selected #19 gate because required interpreter and six-class OS authority coverage is already exercised through mandatory macOS seatbelt plus shell/Python/Node/Ruby paths, with Rscript advisory classification covered separately.
