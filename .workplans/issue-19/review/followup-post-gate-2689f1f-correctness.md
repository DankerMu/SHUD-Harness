Reviewer agent: review-correctness
Review round: post-gate follow-up after V73 fixes
Reviewed head SHA: 2689f1f9bb82b23a86acd51418e40f8fafba3d04
Summary: Not clean; most V73 fixes are covered, but process containment and over-budget command compatibility still have actionable correctness gaps.

Invariant Matrix Coverage:
- Governing raw-byte invariant: covered - seatbelt profile protects canonical raw roots; six escape-class runtime tests and hardlink residual scan evidence are present.
- Legal raw read / workspace write compatibility: missing - over-budget benign commands are pre-denied before sandbox execution.
- Evidence/audit precision: missing - daemonized descendants can mutate workspace/audit paths after the wrapper reports completion.
- Process cancellation/resource cleanup: missing - containment relies on literal session-escape regex plus best-effort descendant sampling.
- env/assignment interpreter hidden denial: covered - assignment/env wrapper tests and prefix normalization are present.
- receiver/cwd/named-mode interpreter targets: covered - Path receiver, named mode, and cwd-aware interpreter cases are tested.
- stderr-file success mask: covered - stderr-to-file hidden denial regression is represented.
- setsid/setpgrp timeout/abort escape: missing - literal `os.setsid` / `os.setpgrp` is rejected, but fragmented/double-fork session escape is not contained.
- delayed audit sabotage: missing - immediate/unwaited background sabotage is covered, but post-return daemonized mutation remains possible.
- cwd false denial: covered - workspace-local `data/raw` writes after cwd changes are allowed.
- running metadata mismatch: covered - final metadata is owned by the outer wrapper.
- fresh project-root audit layout: covered - project root with `data/raw` now resolves to `workspace/tasks/...`.
- Node rename runtime evidence: covered - runtime Node `renameSync` denial case is present.
- unbounded pre-exec scan: missing - scan is bounded, but the budget fallback denies benign over-budget commands rather than only hidden-sensitive forms.
- Zero adapter compatibility: covered - registry wrapping and zero diff evidence are represented.

Findings:
- P1 / Process containment correctness / Invariant: timeout, abort, and normal return must not leave invocation-owned descendants mutating workspace or audit paths / Evidence: `packages/core/src/tools/raw-data-sandbox.ts:3283` checks containment with command-text preflight, `packages/core/src/tools/raw-data-sandbox.ts:3303` only rejects literal `setsid|setpgrp|daemonize|start_new_session|os.setsid|os.setpgrp|Process.daemon`, and `packages/core/src/tools/raw-data-sandbox.ts:1535`-`1624` only samples descendant pids reachable from known pids before killing / Scenario or repro: a bash command can run a Python double-fork with fragmented session escape, for example `python3 -c 'import os,time; p=os.fork(); os._exit(0) if p else None; getattr(os,"set"+"sid")(); p=os.fork(); os._exit(0) if p else None; time.sleep(.3); open("workspace/post-return.txt","w").write("leaked")'`. The literal regex does not match, the parent exits quickly, and the daemonized grandchild can fall outside both the original process group and descendant table before final sampling / Consequence: the tool can return and append `tool.completed` while an invocation-owned process later mutates workspace or audit paths, reopening the V73 process-escape and delayed-audit-sabotage class / Fix direction: do not rely on command-text regex for process containment; either deny daemonization/fork/session escape at the OS sandbox profile level, run commands in a containment primitive with reliable descendant ownership, or fail closed for any process pattern whose descendants cannot be proven reaped before return / Required verification: add normal-return, timeout, and abort regressions using fragmented `setsid`/double-fork descendants that attempt workspace and audit-subtree mutation; assert no mutation after a post-return settle window and final metadata matches the returned ToolResult / Sibling surfaces: `runSeatbeltSandboxedBash`, audit reservation/append, profile protected evidence paths, future RunJob executor, Zero `BashTool` parity / Blocks merge: yes.
- P1 / Compatibility correctness / Invariant: advisory/pre-exec analysis must not reject legal raw reads or workspace writes merely because command semantics are uncertain / Evidence: `packages/core/src/tools/raw-data-sandbox.ts:640`-`654` turns any command-analysis budget failure into `hasHiddenEvidenceRisk: true`, and `packages/core/src/tools/raw-data-sandbox.ts:611`-`616` pre-denies every such case before execution; this path runs even when `enableAdvisory === false` via `packages/core/src/tools/raw-data-sandbox.ts:327`-`345` / Scenario or repro: `cat data/raw/input.csv # ${"x".repeat(130000)}` or a large inline interpreter script that only reads `data/raw/input.csv` and writes `workspace/out.csv` exceeds `COMMAND_ANALYSIS_MAX_LENGTH` or payload budget and is returned as a raw sandbox denial without reaching the OS sandbox / Consequence: legitimate raw reads and workspace writes can be blocked solely due to command size, violating the spec's fail-open advisory boundary and making large generated analysis scripts unusable despite being safe under the seatbelt profile / Fix direction: separate cheap hidden-evidence detection from expensive target analysis; on budget overflow, fail closed only when a bounded pre-scan proves stderr/exit masking or other hidden-denial sensitivity, otherwise allow execution under the OS sandbox and rely on visible denial normalization / Required verification: add over-budget benign raw-read/workspace-write tests that succeed, plus over-budget hidden raw-write tests that still fail closed before side effects / Sibling surfaces: `evaluateSuppressedSandboxFailureGuard`, `evaluateRawDataWriteAdvisory`, post-exec denial normalization, large generated bash/R/Python scripts / Blocks merge: yes.

Non-blocking notes:
- I did not rerun the reported test suite; this pass was read-only static review against the diff, implementation, tests, and OpenSpec refs.
- Good patterns retained: raw denial payload/audit row/event-input builders share one payload source, audit handles reject symlink/hardlink targets, and running metadata is finalized by the outer wrapper.

Execution Summary: agents=review-correctness; skills=review; tools=git/rg/sed/nl read-only; verification=static diff+spec+test inspection; limits=no test execution/no file edits.
