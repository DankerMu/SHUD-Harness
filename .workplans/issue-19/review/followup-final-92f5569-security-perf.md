Reviewer agent: review-security-perf
Review round: final comprehensive follow-up 92f5569
Reviewed head SHA: 92f556915416a57015dcaa32ca97e044c9fc3353
Summary: Raw byte authority, trusted telemetry narrowing, CI gating, and b999d2e fixes are covered; one P2 lifecycle hardening gap remains for malformed custom evaluator raw-deny returns.

Invariant Matrix Coverage:
- Governing invariant: covered - `RawDataSandboxedBashTool` builds a macOS seatbelt profile denying `file-write*` on canonical protected raw paths and protected evidence paths while allowing reads and configured write roots; execution tests cover create/modify/delete/rename/truncate preservation in `packages/core/src/tools/raw-data-sandbox.test.ts`.
- Source-of-truth identity/contract: covered - OpenSpec条 2' and ADR-0001 now define byte authority plus trusted observable telemetry; implementation uses `RAW_DATA_WRITE_RULE_ID`, `RAW_DATA_SANDBOX_PROFILE_VERSION`, remediation payloads, `tool.failed`, and fixture audit path.
- Producers: covered - producers are present in `packages/core/src/tools/raw-data-sandbox.ts`, `packages/core/src/tools/policy-gate-registry.ts`, and `packages/backend/src/ws/index.ts`.
- Validators/preflight: covered - profile construction, advisory rule, hardlink scanner, process containment preflight, audit path safety, and sandbox execution tests are in `raw-data-sandbox.test.ts` and `policy-gate-registry.test.ts`.
- Storage/cache/query: covered - profile files are created under bounded temp/profile roots and cleaned with identity checks; audit rows are reserved under `workspace/tasks/TASK-M1-SPIKE/audit/` with no-follow/hardlink checks.
- Public routes/entrypoints: covered - no full WS route is introduced; only the skeleton `tool.failed` builders exist in `packages/backend/src/ws/index.ts`.
- Frontend/downstream consumers: covered - `tool.failed` envelope shape includes `seq`, `event_id`, remediation-bearing `ErrorRecord`, and trusted raw advisory fields; generic builder rejects raw-denial-shaped forged events.
- Failure paths/rollback/stale state: partially covered - raw mutation failure paths are covered and post-exec output is no longer promoted to `denied_by_sandbox`; malformed custom evaluator raw-deny returns still have a lifecycle gap noted below.
- Evidence/audit/readiness: covered - GitHub CI for this SHA passed `linux-base`, `macos-seatbelt`, and aggregate `check`; zero remains pinned at `13e25c116c62411e6ee8a0ad67a6c53dc7c376c6`.
- Six escape classes: covered - interpreter payload, pipeline/stdin, dynamic target, shell state/child process, symlink/`../`, and rename/unlink are represented in seatbelt tests; hidden/suppressed denials assert raw bytes remain unchanged without false sandbox-denial telemetry.
- Raw read and workspace write: covered - raw reads, workspace writes, raw-read copy-to-workspace, and waited foreground subprocess workspace writes are tested as allowed.
- Pre-existing hardlink residual: covered - test demonstrates mutation through a pre-existing hardlink alias and verifies `scanProtectedHardlinks` scans only explicit protected roots with a path-count budget.
- Advisory denial: covered - obvious static raw writes produce remediation/audit/trusted evidence; over-budget or uncertain commands fail open to OS authority and legal reads are not advisory-denied.
- Zero unchanged: covered - CI verifies `git -C zero diff --quiet` and the pinned SHA.

Findings:
- Severity: P2 / Failure class: error handling / state-transition / Contract or invariant: policy evaluator and remediation failures should fail closed as failed `ToolResult`s, skip inner execution, run deny-style post-processing, and finish running handles; outer `RAW_DATA_WRITE_RULE_ID` evaluator ownership must fail closed as configuration misuse. / Scenario or repro: a custom `PolicyGateEvaluator` returns `{ decision: "deny", ruleId: RAW_DATA_WRITE_RULE_ID, reason: "bad" } as any` or a malformed remediation object. The wrapper has already left the `try` around `evaluate`, then enters the raw-rule branch and dereferences `decision.remediation.ref`, throwing instead of returning the intended failed `ToolResult`; a registered running handle is not finalized by `finalizePolicyGateResult`. / Evidence: `packages/core/src/tools/policy-gate-registry.ts:241`, `packages/core/src/tools/policy-gate-registry.ts:273`, `packages/core/src/tools/policy-gate-registry.ts:357`. / Consequence: malformed custom evaluator configuration can still produce a rejected tool call and stale running-tool lifecycle metadata. It does not execute the inner tool or weaken raw byte authority, but it leaves one fail-closed lifecycle path outside the b999d2e fix. / Fix direction: validate or normalize the returned `PolicyGateDecision` before branching, and wrap deny-result construction in the same `finalizePolicyGateResult` failure conversion. For raw-rule misuse, synthesize a valid `fix_and_retry` remediation even when the evaluator-provided remediation is absent or invalid. / Required verification: add focused tests for a custom evaluator returning `RAW_DATA_WRITE_RULE_ID` deny with missing remediation and invalid remediation, asserting failed `ToolResult`, no inner execution, deny-style post-processing, and finished running handle. / Sibling surfaces: generic `buildPolicyGateDeniedResult`, `createShudRuntimeToolRegistry({ evaluate })`, `wrapToolWithPolicyGate`, and policy-gate registry tests. / Blocks merge: no.

Non-blocking notes:
- GitHub CI evidence for `92f5569` is green for `linux-base`, `macos-seatbelt`, and aggregate `check`.
- I did not rerun local tests to preserve the leaf review read-only boundary.
