Reviewer agent: review-security-perf
Review round: post-gate follow-up after V73 fixes
Reviewed head SHA: 2689f1f9bb82b23a86acd51418e40f8fafba3d04
Summary: Not clean; dynamic detached subprocesses can still escape lifecycle containment and corrupt audit truth after the tool returns success.

Invariant Matrix Coverage:
- Governing raw-byte invariant: covered - direct raw write/delete/rename/symlink forms are enforced by the seatbelt profile and tests cover those paths.
- Process containment and cleanup: missing - containment relies on static string signals plus PPID descendant discovery; dynamically created new-session children can reparent before teardown.
- Audit path protection and evidence durability: missing - a detached child can move `workspace/tasks` after `tool.completed` is appended, removing the canonical audit path after success.
- Denial evidence synchronization: missing - delayed raw-write attempts by escaped children are blocked by seatbelt, but the wrapper records `decision=allowed` and emits no `denied_by_sandbox` evidence.
- Scan budgets and adversarial command analysis: covered - command length, segment count, interpreter payload length, call count, and hardlink scan budgets are now bounded.
- Hardlink residual handling: covered - pre-existing hardlink residual is documented and `nlink>1` scan is explicit-root bounded.
- Info leakage: covered - no new blocking leakage found in the changed surfaces; profile path exposure appears intentional in current audit/test contracts.
- Cross-check analogous helper surfaces: covered - registry replacement wraps `bash` through `RawDataSandboxedBashTool`; no sibling surface provides stronger runtime process containment.

Findings:
- P1 / Process containment + audit evidence durability / Contract or invariant: Issue #19 requires child-process inheritance, resource cleanup, and denial evidence to reflect raw write attempts; detached descendants must not mutate workspace or audit state after return / Evidence: `packages/core/src/tools/raw-data-sandbox.ts:3283` only pre-denies literal `setsid|setpgrp|start_new_session` strings, `packages/core/src/tools/raw-data-sandbox.ts:1146` waits for only the top-level spawned process before final cleanup, `packages/core/src/tools/raw-data-sandbox.ts:1647` discovers descendants only by current PPID ancestry, and `packages/core/src/tools/raw-data-sandbox.ts:424` appends `allowed` after the parent result / Scenario or repro: targeted probe with `python3 -c 'import subprocess; kw={"start"+"_new_session": True}; subprocess.Popen(["sh","-c","sleep 0.25; printf leaked > workspace/popen-session-leak.txt"], stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL, **kw)'` returned `success: true`, then wrote `workspace/popen-session-leak.txt` after return; the same pattern with `printf nope > data/raw/delayed-raw.txt` left raw bytes protected but produced only a `tool.completed`/`decision=allowed` audit row; the same pattern with `mv workspace/tasks workspace/tasks.moved` removed the canonical audit subtree after the allowed row / Consequence: callers receive a successful bash result while untracked children continue running; raw write attempts can be silently denied without `tool.failed`/`denied_by_sandbox` evidence, and canonical audit history can be moved after the wrapper reports success / Fix direction: do not rely on command-string regex for daemon/session escape; either supervise in a way that can kill/reap reparented new-session descendants before returning, or fail closed for interpreter/subprocess/daemonizing forms that cannot be proven foreground-contained; also protect or revalidate the audit ancestor after a post-return settle window / Required verification: add regression tests for dynamically constructed `start_new_session`, `subprocess.Popen`, double-fork/reparent, delayed raw write, delayed workspace write, and delayed `workspace/tasks` move; assert failed tool result or containment failure, no post-return side effects, and no false `allowed` audit row / Sibling surfaces: timeout path, abort path, normal successful completion path, audit reservation/append identity checks, `createShudSandboxedBashTool`, future RunJob sandbox execution / Blocks merge: yes.

Non-blocking notes:
- Targeted probes were run with `pnpm --package=bun@1.2.19 dlx bun --eval`; full suite was not rerun in this review.
- Execution Summary: agents=review-security-perf; skills=review; tools=gh, git, rg, sed/nl, pnpm dlx bun; verification=targeted repro probes; limits=read-only, no edits/commits/push.
