# Issue #91 Round 2 source-bound red proof

Date: 2026-07-26

This replay was run in an isolated detached worktree created at repaired commit
`ba4aff099e45bbac483eaf2cdf33cdebe98281e8`. The Round 2 tests and repository
fixture stayed at that commit while only the two changed production sources were
restored from pre-repair commit
`deafe41fe444bdb9f3cdab4dbad1c0829e0778ab`.

## Exact source and test identities

| Path | Pre-repair blob (`deafe41f`) | Repaired/test blob (`ba4aff0`) |
|---|---|---|
| `packages/core/src/domain/services/stack-lock-collector.ts` | `21cf17d27bf17a2d20a195cd9071fdd3b5964423` | `19b3f7d8c748cafd2708779c8d2bb5cf8ecbb4ae` |
| `packages/core/src/domain/services/hashing-service.ts` | `f8b3a224cb685cd84f6465b1f1e2689dea4df135` | `a032a5eb15237c2f83385c30334a5bab9543cdc3` |
| `packages/core/src/domain/services/stack-lock-collector.test.ts` | `c5da282c4b73ade874ec986f9af81690b37543cf` | `c68f1c86c157964339a3feb345b61ff8eb9c0ec4` |
| `packages/core/src/domain/services/hashing-service.test.ts` | `a43d4b21405a06b3e51faa82962047e765502498` | `70168410e6cbe22057f91b8653a402da837f662e` |
| `.gitmodules` | `31f5151f141c79e63c8ae684437eeb3b71f8a302` | `16a50eecbe8e211a508874ba248c80ed73651d85` |

The test command was exactly:

```text
npx --yes bun@1.2.19 test packages/core/src/domain/services/stack-lock-collector.test.ts packages/core/src/domain/services/hashing-service.test.ts
```

## Red replay

Exit status: `1`. Result: `49 pass`, `15 fail`, `186 expect() calls`.

All failing test names and the production behavior they detect:

| Failing test | Repair contract proved |
|---|---|
| `collects four gitlinks, explicit placeholders, provider identity, and missing-renv degradation` | branch authority must project zero=`development`, not the old constant `main` |
| `rejects .gitmodules byte-only drift across the generation barrier` | raw `.gitmodules` identity participates in revalidation |
| `rejects .gitmodules branch-authority drift across the generation barrier` | branch declaration changes cannot publish a mixed snapshot |
| `rejects stable .gitmodules missing declaration with a typed non-disclosing error` | exact four-declaration inventory is required |
| `rejects stable .gitmodules duplicate declaration with a typed non-disclosing error` | duplicate declarations fail closed |
| `rejects stable .gitmodules unknown declaration with a typed non-disclosing error` | unknown declarations fail closed |
| `rejects stable .gitmodules mismatched path with a typed non-disclosing error` | declared path is validated against the exact repository key |
| `rejects stable .gitmodules mismatched branch with a typed non-disclosing error` | declared branch is validated against version-controlled authority |
| `default Git wrapper derives a minimal non-secret child environment` | ambient credentials/config/repository redirects are not forwarded |
| `maps the 16 MiB renv.lock descriptor bound to renv_lock_invalid` | collector applies the 16 MiB opened-descriptor bound |
| `does not start renv hashing when an earlier bounded producer fails` | cheap bounded producers complete before the expensive hash |
| `hostile inherited Git trace sinks are not created by real collection` | inherited trace sinks cannot cause file writes |
| `a local promisor fixture with a missing tree fails without lazy fetch or pack growth` | a missing object cannot trigger lazy fetch or pack growth |
| `default git reader observes the real four gitlinks including the frozen zero pin without git mutation` | real repository projection uses declared branches and preserves Git state |
| `hashFile accepts an exact descriptor byte bound and rejects bound plus one before content reads` | shared file hashing enforces the file-only descriptor bound before reads |

The failure output was generated by the command above; no historical output was
reconstructed. In particular, the old sources returned zero branch `main`,
accepted invalid/changed `.gitmodules`, forwarded the hostile environment,
accepted 16 MiB+1, created trace output, allowed promisor lazy fetch, lacked the
ordering seam, and read the oversized hashing probe once.

## Restored green replay and hygiene

Both production files were restored from `ba4aff099e45bbac483eaf2cdf33cdebe98281e8`
and the same command was rerun. Exit status: `0`; result: `64 pass`, `0 fail`,
`246 expect() calls`.

Before cleanup, `git diff --exit-code`, `git diff --cached --exit-code`, and
`git status --short` were clean after the temporary dependency symlink was
removed. `git stash list` was empty throughout; no `red-proof` stash was created.
The detached worktree and its empty temporary parent were then removed, and
`git worktree list --porcelain` contained no `issue91-redproof` entry.

## Depth-retro boundary-test bite proof

The later exact-16-MiB collector regression was independently proved by changing
only `STACK_LOCK_RENV_MAX_BYTES` from `16 * 1024 * 1024` to
`16 * 1024 * 1024 - 1`. The exact focused test
`hashes an exact 16 MiB regular renv.lock against an independent SHA256 oracle`
failed with `renv_lock_invalid` (`0 pass`, `1 fail`). The one-line production
mutation was immediately restored; the complete collector suite then passed
with `49 pass`, `0 fail`, and `173 expect() calls`.

The root-identity/global-argv regressions were also run with only
`stack-lock-collector.ts` stashed back to exact workflow HEAD
`a10e88a6d6493990972ce191253f4d75a6dd79de`, retaining the new tests. The exact
focused name pattern covered the ordinary exact-argv row, old-client row,
nested-root row, and second-root-observation row. Exit status was `1`; all four
tests failed (`0 pass`, `4 fail`) because the old source issued only two
`ls-tree` calls, classified the simulated old client as malformed gitlink
output, and published results from nested or root-drifted inputs. The stash was
immediately popped and dropped; the identical four-test command then reported
`4 pass`, `0 fail`, and `30 expect() calls`, with no matching red-proof stash.
